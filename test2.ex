# For the numbers 1 - 100, print Fizz if it is divisible by 3, Buzz if it is divisible by 5, FizzBuzz if divisible by 3 and 5
# and just print the number if none of those are true
#
# Can use `h rem` to find divisible

defmodule FizzBuzz do
end
